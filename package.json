{
  "name": "ntou-helper",
  "version": "1.0.1",
  "description": "NTOU Space Booking Helper (Local Extension)",
  "scripts": {
    "build": "tsc && node build.js",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.263",
    "adm-zip": "^0.5.12",
    "typescript": "^5.4.5"
  }
}
